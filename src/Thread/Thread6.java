package Thread;

public class Thread6 {
}
//쓰레드 그룹
//-서로 관련된 쓰레드를 그룹으로 묶어서 다루기 위한 것
//-모든 쓰레드는 반드시 하나의 쓰레드 그룹에 포함되어 있어야 한다.
//-쓰레드 그룹을 지정하지 않고 생성한 쓰레드는 'main쓰레드 그룹'에 속한다.
//-자신능 생성한 쓰레드(부모 쓰레드)의 그룹과 우선순위를 상속받는다.